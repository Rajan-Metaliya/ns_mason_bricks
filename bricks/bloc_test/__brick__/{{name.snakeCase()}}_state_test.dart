import 'package:flutter_test/flutter_test.dart';

void main() {

    group('{{name.pascalCase()}}State', () {

      test("{{name.pascalCase()}}State -> toString", () {
        expect(
          {{name.pascalCase()}}State().toString(),
          "{{name.pascalCase()}}State",
        );
      });

    });
}